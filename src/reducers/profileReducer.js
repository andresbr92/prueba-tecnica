import {
    LOGIN_SUCCESS,
    LOGIN_FAILURE

} from '../types'

const initialState = {
    profile: null,
    isLoggedIn: null,
    loading: false
}

export default function (state = initialState, action) {
    switch (action.type) {
        default: 
            return state
    }
}